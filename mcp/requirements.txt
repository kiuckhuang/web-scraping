mcp==2.2.0
httpx==0.28.1
uvicorn==0.53.0
# Pinned explicitly: mcp 2.2.0 only sets lower bounds (starlette>=0.27,
# pyjwt>=2.10.1), so without these the image silently pulls transitive deps
# that may predate the security fixes (Starlette CVE-2026-48710 "BadHost" +
# CVE-2026-48817/48818/54282/54283; PyJWT CVE-2026-32597 + the 48522..48526
# cluster fixed in 2.13.0, and the 2.14.0 hardening batch — HMAC key
# validation vs JWK/JWKS/PEM confusion (GHSA-r6x4-923q-g947 et al.),
# PyJWKClient redirect + refresh-flood (GHSA-9v7f-9g4p-ffgj, GHSA-2gx3-rcp4-g85q)).
starlette==1.6.0
pyjwt[crypto]==2.14.0
